<?xml version="1.0" encoding="UTF-8"?>
<ruleset name="DrupalCustom">
  <description>Custom Drupal coding standards for search_api_postgresql</description>

  <!-- Use Drupal core standards -->
  <rule ref="Drupal">
    <!-- Exclude problematic Slevomat rules -->
    <exclude name="SlevomatCodingStandard.Classes.BackedEnumTypeSpacing"/>
    <exclude name="SlevomatCodingStandard.ControlStructures.RequireNullCoalesceOperator"/>
    <exclude name="SlevomatCodingStandard.PHP.ShortList"/>
    <exclude name="SlevomatCodingStandard.TypeHints.DeclareStrictTypes"/>
    <exclude name="SlevomatCodingStandard.TypeHints.NullableTypeForNullDefaultValue"/>
  </rule>

  <!-- Use DrupalPractice as well -->
  <rule ref="DrupalPractice"/>

  <!-- File patterns to check -->
  <file>./src</file>
  <file>./tests</file>

  <!-- File extensions -->
  <arg name="extensions" value="php,module,inc,install,test,profile,theme"/>

  <!-- Show progress -->
  <arg value="p"/>
</ruleset>